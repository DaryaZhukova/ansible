- name: Generate key
  hosts: local
  pre_tasks:
  - name: Create fact folder
    become: yes
    file:
      path: /etc/ansible/facts.d
      state: directory


- name: Configuring user
  hosts: appservers
  pre_tasks:
  - name: Create fact folder
    become: yes
    file:
      path: /etc/ansible/facts.d
      state: directory
  roles:
  - jenkins

